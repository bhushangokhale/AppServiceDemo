name: manage-azure-policy-9cf783b6
on:
  workflow_dispatch:
    inputs:
      remarks:
        description: 'Reason for triggering the workflow run'
        required: false
        default: 'Updating Azure Policies'


## Uncomment the following if you wish to automatically trigger workflow run with every commit
#
#  push:
#    paths:
#      - 'policies/Contoso_-_Deny_SQMI_Public_Endpoints_264fdd0a-c8c1-4d52-a33f-b0e4ccf8591c/**'
#      - .github/workflows/manage-azure-policy-9cf783b6.yml
jobs:
  apply-azure-policy:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout
      uses: actions/checkout@v2
    - name: Login to Azure
      uses: azure/login@v1
      with:
        creds: ${{secrets.AZURE_CREDENTIALS_9cf783b6}}
        allow-no-subscriptions: true
    - name: Create or Update Azure Policies
      uses: azure/manage-azure-policy@v0
      with:
        paths: |
          policies/Contoso_-_Deny_SQMI_Public_Endpoints_264fdd0a-c8c1-4d52-a33f-b0e4ccf8591c/**
